TODO

- volumes don't persist on redeploy
- stop restarting entire emulator container when changing app src
- refreshing after a while causes an error message and no banner appears
- test everything

TODO (LOW PRIORITY)

- investigate "vite not found" in production build (extra RUN npm install as workaround)
- investigate error on signup
- fix express session types (i.e. session.token)
- fix env var types (don't allow undefined)

- fix sass errors on build
- migrate to postgres
- cron job to run certbot renewal
